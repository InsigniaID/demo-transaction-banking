services:
  web:
    image: ex-fastapi:1.0.0
    container_name: ex-fastapi
    command: ["pipenv", "run", "uvicorn", "apps.main:app", "--reload", "--host", "0.0.0.0", "--port", "8001"]
    volumes:
      - .:/app
    ports:
      - "8001:8001"